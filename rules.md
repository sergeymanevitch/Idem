# Rules

Not written yet. This file will hold the procedure — the order of work, the self-check, the
prohibitions — and each step will name the reference file it depends on. It will hold no list and
no definition; those live in `reference/`.
