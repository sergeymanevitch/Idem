# 00_tickets — translator output

Written only by the translator, one `<snapshot-stem>.tickets.md` per snapshot. No hook is built: a
`*.tickets.md` here is validated by running `02_validate/validate.py` on it by hand. Empty until
the first translation.
