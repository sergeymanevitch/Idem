# 00_tickets — translator output

Written only by the translator, one `<snapshot-stem>.tickets.md` per snapshot. The Claude Code hooks
validate every `*.tickets.md` here. Empty until the first translation.
